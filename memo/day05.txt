input 태그의 주요 속성
- autofocus
    페이지에 진입시 자동으로 focus됨 
    마우스커서가 해당 input에서 깜박임
    
- readonly
    읽기 전용 필드로 설정함
    사용자가 입력할 수 없음
    
- disabled
    해당 태그를 비활성화 시키며 필드 데이터를 서버에 전송하지 않음
    
- placeholder
    input 에 힌트를 표시함
    ex) 아이디를 입력하는 칸에 ‘아이디를 입력하세요’ 등의 문구를 띄움
    
- required
    필수 필드로 설정한다. (반드시 입력해야함)
    필드의 데이터들을 submit 버튼을 눌러서 전송할 때 필수 필드에 데이터가 있는지 확인한다.
    
- value
    텍스트를 입력하는 input 에 미리 데이터를 채울 수 있다.
    submit 버튼의 텍스트를 변경할 수도 있다.
    
- name
    서버쪽에 데이터를 보낼때 데이터가 무슨 데이터인지 이름을 붙여 보낸다.

============================================================

textarea 태그
    <textarea cols = "가로너비" rowe ="세로길이"> 내용</textarea>\
    택스트를 여러 줄 입력하는 영역을 만드는 태그는

    select, option 태그
        여러 옵션 중 하나를 선탹하는 드롭다운 목록을 만드는 태그는

button 태그
    <bottom tyep ="submit | reset | bottom">내용</bottom>
    버튼을 만드는 태그
    ** button태그의 type를 생략하면 submit으로 설정된다.

    반응형(responsive)웹
        최근 웹사이트를 pc뿐만 아니라 모바일, 태블릿 같은 기기로 접속하는 경우가 많다.
        이런 경우 기기화면에 맞춰서 새로운 사이트를 만드는 것은 비효율적
        그래서 사용자의 화면 크기에 반으앟는 사이트를 만들게 된다
        이러한 사이트를 반응형 웹 사이트라 한다
        기존 내용을 유지하면서 사용자의 화면 크기에 맞게 재배치 하고 다시 표현한다

        ewport(뷰포트)
        뷰포트는 


        CSS의 우선순위

            1. 속성값 뒤에 !important를 붙인 경우
            2, 인라인 스타일을 적용한 경우
            3. 선택자로 id 를 사용한 경우
            4. 선택자로 class를 사용한 경우
            5. 선택자로 teg를 사용한 경우

            우선순위가 같다면 선택자를 상세하게 작성한 css가 적용된다.
            선택자도 동일하다면 나중에 작성한 css가 적용된다.